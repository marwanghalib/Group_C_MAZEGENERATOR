#ifndef MAZE_H
#define MAZE_H
#include "maze_h"
#endif

/**
  This funcion will generate the
  maze based sidewinder algorithm
*/
inline void gen_sidewinder(maze_t* maze);

/**
  This funcion will generate the
  maze based on backtrack algorithm
*/
inline void gen_backtrack(maze_t* maze);
