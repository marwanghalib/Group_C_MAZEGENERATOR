#ifndef STDIO_H
#define STDIO_H

#include <stdio.h>

#endif /* STDIO_H */

#ifndef MAIN_H
#define MAIN_H

#include <main.h>

#endif /* MAIN_H */


int main(int argc, char* argv[]){
    printf("There are %d arguments.\nThey are the following:\n", argc);
    for(int i = 0; i<argc; i++){
        printf("%d: %s\n", i, argv[i]);
    }
    return 0;
}
